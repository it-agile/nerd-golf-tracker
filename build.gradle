apply plugin: 'java'

defaultTasks 'build'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

jar {
    manifest {
        attributes('Main-Class': 'de.itagile.golf.Main')
    }
}